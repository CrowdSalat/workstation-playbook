---
# DNF packages installed inside the toolbox.
toolbox_dnf_packages:
  - nodejs

# npm packages installed globally inside the toolbox (requires sudo).
toolbox_npm_packages:
  - "@earendil-works/pi-coding-agent"
