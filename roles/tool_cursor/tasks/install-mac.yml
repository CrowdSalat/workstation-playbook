---
- name: Install Cursor via Homebrew package manager role
  ansible.builtin.include_role:
    name: package_manager_brew
  vars:
    package_manager_brew_casks:
      - "{{ tool_cursor_macos_cask }}"
