---
- name: Ensure user directories exist
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: "0755"
  loop:
    - "{{ local_bin }}"
    - "{{ local_share_apps }}"
    - "{{ applications_dir }}"
